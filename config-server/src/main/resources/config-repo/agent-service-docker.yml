spring:
  datasource:
    url: ${AGENT_DB_URL:jdbc:mysql://mysql:3306/sc_agent?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${AGENT_DB_USERNAME:dev_user}
    password: ${AGENT_DB_PASSWORD:dev_only_pwd}

agent:
  llm:
    enabled: ${AGENT_LLM_ENABLED:false}
