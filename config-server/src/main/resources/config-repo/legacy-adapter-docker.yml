spring:
  datasource:
    url: ${LEGACY_DB_URL:jdbc:mysql://mysql:3306/major_assignment?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
    username: ${LEGACY_DB_USERNAME:dev_user}
    password: ${LEGACY_DB_PASSWORD:dev_only_pwd}
