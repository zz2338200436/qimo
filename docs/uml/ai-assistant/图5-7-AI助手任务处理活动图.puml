@startuml
!include 00-style.puml

|用户|
start
:输入目标或问题;

|前端页面|
:提交请求与上下文;
if (参数完整?) then (是)
else (否)
  :提示补充信息;
  stop
endif

|助手服务|
:校验身份与角色;
:识别意图;
:装载会话上下文;
:检索知识片段;
if (需要工具?) then (是)
  :规划工具调用;
  if (高风险操作?) then (是)
    :等待人工确认;
    if (确认执行?) then (确认)
    else (取消)
      :记录取消审计;
      stop
    endif
  endif
  |模型/工具服务|
  :执行工具/查询外部服务;
  if (工具成功?) then (是)
  else (否)
    |助手服务|
    :生成失败说明或降级方案;
    :记录失败审计;
    stop
  endif
endif

|模型/工具服务|
:模型生成回答;

|助手服务|
:合成最终结果与引用来源;
:记录会话、工件、审计日志;

|用户|
:查看并采纳结果;
stop

@enduml
