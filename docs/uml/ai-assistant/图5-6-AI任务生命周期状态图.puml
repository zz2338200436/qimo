@startuml
!include 00-style.puml

[*] --> 新建
新建 --> 上下文装载 : 创建会话/保存请求
上下文装载 --> 规划中 : 识别意图/补全槽位
规划中 --> 生成中 : 直接回答或调用模型
规划中 --> 待人工确认 : 高风险工具调用
生成中 --> 已完成 : 回答成功
待人工确认 --> 生成中 : 用户确认执行
待人工确认 --> 已取消 : 用户取消
生成中 --> 失败 : 服务异常/超时
失败 --> 重试中 : 满足重试策略
重试中 --> 生成中 : 重新执行
失败 --> 降级完成 : 返回静态或回退结果
生成中 --> 超时降级 : 超过时限
超时降级 --> 已完成 : 输出降级回答
已完成 --> 已归档 : 审计完成/会话结束
已取消 --> 已归档
降级完成 --> 已归档

@enduml
