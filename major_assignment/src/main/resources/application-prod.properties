# prod Profile 覆盖配置（R4.3、R4.4）
# 所有敏感字段必须由环境变量注入，不得在仓库中保留任何生产默认值。
# 若缺失关键变量，ProdConfigValidator 将在启动阶段快速失败（System.exit(1)）。

# 数据库凭证（无默认 —— 必须由运行环境注入）
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Redis 凭证（无默认）
spring.data.redis.password=${REDIS_PASSWORD}
