spring:
  application:
    name: user-service
  config:
    import: optional:classpath:application-common.yml
  datasource:
    url: ${USER_DB_URL:${DB_URL:jdbc:mysql://localhost:3306/sc_user?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}}
    username: ${USER_DB_USERNAME:${DB_USERNAME:dev_user}}
    password: ${USER_DB_PASSWORD:${DB_PASSWORD:dev_only_pwd}}
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: ${USER_FLYWAY_ENABLED:true}
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: ${EUREKA_PREFER_IP_ADDRESS:false}
    hostname: ${EUREKA_INSTANCE_HOSTNAME:localhost}
    ip-address: ${EUREKA_INSTANCE_IP:127.0.0.1}
